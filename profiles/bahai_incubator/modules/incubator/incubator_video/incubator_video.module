<?php
// $Id$

/**
 * @file
 *
 */

function incubator_video_media_browser_plugins_alter(&$plugins) {
  if ($plugins['library']['#settings']['file_directory'] == 'video') {
    unset($plugins['upload']);
  }
}